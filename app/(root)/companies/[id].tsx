import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const CompanyDetailsScren = () => {
  return (
    <SafeAreaView>
      <Text>CompanyDetailsScren</Text>
    </SafeAreaView>
  );
};

export default CompanyDetailsScren;
